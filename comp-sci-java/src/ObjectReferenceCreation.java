public class ObjectReferenceCreation {
    public static void main(String[] args) {
        ObjectReferenceCreation a = new ObjectReferenceCreation(),
                                b = new ObjectReferenceCreation(); // this declares two reference variables.
        /* two objects are created.
        a & b are object reference variables.
        baris pertama, object dibentuk dengan referensi ke a,
        baris kedua, object dibentuk dengan referensi ke b.
        */
    }
}
